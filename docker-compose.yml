version: '3.8'
services:
  app:
    restart: always
    container_name: online-judge
    build:
      context: ./
    command: npm run start:dev
    volumes:
      - ./:/app
      - /app/node_modules
      - uploads:/app/uploads
    ports:
      - '3000:3000'

volumes:
  uploads:
